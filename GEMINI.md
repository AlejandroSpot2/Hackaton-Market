# GEMINI.md

RealityCheck AI turns a startup idea into a navigable market atlas.

Working rules:
- the atlas is the primary artifact
- mocked fixtures are acceptable for the first pass
- FastAPI owns the run lifecycle contract
- Next.js only handles UX and visualization
- Prefect stays in the orchestration layer
- persist every run and partial result to local JSON

Start with `AGENTS.md`, then `CONTEXT.md`, then `README.md`.